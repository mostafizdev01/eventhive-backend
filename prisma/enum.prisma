enum UserRole {
    USER
    HOST
    ADMIN
}

enum UserStatus {
    ACTIVE
    INACTIVE
    DELETED
}

enum Gender {
    MALE
    FEMALE
}

enum PaymentStatus {
    PAID
    FAILED
    REFUNDED
}

enum TicketStatus {
    CONFIRMED
    CANCELLED
}

enum EventStatus {
    PENDING
    APPROVED
    CANCELLED
}
