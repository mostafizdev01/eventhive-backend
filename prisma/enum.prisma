enum UserRole {
    USER
    HOST
    ADMIN
}

enum UserStatus {
    ACTIVE
    INACTIVE
    DELETED
}

enum Gender {
    MALE
    FEMALE
}

enum PaymentStatus {
    PAID
    UNPAID
    FAILED
    REFUNDED
}

enum TicketStatus {
    CONFIRMED
    PENDING
    CANCELLED
}

enum EventStatus {
    OPEN
    CLOSE
    CANCELLED
}
